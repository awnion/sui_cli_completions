
use builtin;
use str;

set edit:completion:arg-completer[sui] = {|@words|
    fn spaces {|n|
        builtin:repeat $n ' ' | str:join ''
    }
    fn cand {|text desc|
        edit:complex-candidate $text &display=$text' '(spaces (- 14 (wcswidth $text)))$desc
    }
    var command = 'sui'
    for word $words[1..-1] {
        if (str:has-prefix $word '-') {
            break
        }
        set command = $command';'$word
    }
    var completions = [
        &'sui'= {
            cand -h 'Print help'
            cand --help 'Print help'
            cand start 'Start a local network in two modes: saving state between re-runs and not saving state between re-runs. Please use (--help) to see the full description.'
            cand network 'network'
            cand genesis 'Bootstrap and initialize a new sui network'
            cand genesis-ceremony 'genesis-ceremony'
            cand keytool 'Sui keystore tool'
            cand client 'Client for interacting with the Sui network'
            cand validator 'A tool for validators and validator candidates'
            cand move 'Tool to build and test Move applications'
            cand bridge-committee-init 'Command to initialize the bridge committee, usually used when running local bridge cluster'
            cand fire-drill 'Tool for Fire Drill'
            cand analyzer 'Invoke Sui''s move-analyzer via CLI'
            cand analyze-trace 'Analyze and/or transform a trace file'
            cand replay 'Arguments for replay (used for both CLI and config file)'
            cand completion 'Generate shell completion scripts for CLI'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;start'= {
            cand --network.config 'Config directory that will be used to store network config, node db, keystore. sui genesis -f --with-faucet generates a genesis config that can be used to start this process. Use with caution as the `-f` flag will overwrite the existing config directory. We can use any config dir that is generated by the `sui genesis`'
            cand --with-faucet 'Start a faucet with default host and port: 0.0.0.0:9123. This flag accepts also a port, a host, or both (e.g., 0.0.0.0:9123). When providing a specific value, please use the = sign between the flag and value: `--with-faucet=6124` or `--with-faucet=0.0.0.0`, or `--with-faucet=0.0.0.0:9123`'
            cand --with-indexer 'Start an indexer with a PostgreSQL database'
            cand --with-consistent-store 'Start a Consistent Store with default host and port: 0.0.0.0:9124. This flag accepts also a port, a host, or both (e.g., 0.0.0.0:9124)'
            cand --with-graphql 'Start a GraphQL server with default host and port: 0.0.0.0:9125. This flag accepts also a port, a host, or both (e.g., 0.0.0.0:9125)'
            cand --fullnode-rpc-port 'Port to start the Fullnode RPC server on. Default port is 9000'
            cand --epoch-duration-ms 'Set the epoch duration. Can only be used when `--force-regenesis` flag is passed or if there''s no genesis config and one will be auto-generated. When this flag is not set but `--force-regenesis` is set, the epoch duration will be set to 60 seconds'
            cand --data-ingestion-dir 'Make the fullnode dump executed checkpoints as files to this directory. This is incompatible with --no-full-node'
            cand --committee-size 'Set the number of validators in the network. If a genesis was already generated with a specific number of validators, this will not override it; the user should recreate the genesis with the desired number of validators'
            cand --force-regenesis 'A new genesis is created each time this flag is set, and state is not persisted between runs. Only use this flag when you want to start the network from scratch every time you run this command'
            cand --no-full-node 'Start the network without a fullnode'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;network'= {
            cand --network.config 'network.config'
            cand -d 'Dump the public keys of all authorities'
            cand --dump-addresses 'Dump the public keys of all authorities'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis'= {
            cand --from-config 'Start genesis with a given config file'
            cand --write-config 'Build a genesis config, write it to the specified path, and exit'
            cand --working-dir 'working-dir'
            cand --epoch-duration-ms 'epoch-duration-ms'
            cand --benchmark-ips 'A list of ip addresses to generate a genesis suitable for benchmarks'
            cand --committee-size 'Set number of validators in the network'
            cand -f 'Forces overwriting existing configuration'
            cand --force 'Forces overwriting existing configuration'
            cand --with-faucet 'Creates an extra faucet configuration for sui persisted runs.'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony'= {
            cand --path 'path'
            cand --protocol-version 'protocol-version'
            cand -h 'Print help'
            cand --help 'Print help'
            cand init 'init'
            cand validate-state 'validate-state'
            cand add-validator 'add-validator'
            cand list-validators 'list-validators'
            cand build-unsigned-checkpoint 'build-unsigned-checkpoint'
            cand examine-genesis-checkpoint 'examine-genesis-checkpoint'
            cand verify-and-sign 'verify-and-sign'
            cand finalize 'finalize'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;genesis-ceremony;init'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;validate-state'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;add-validator'= {
            cand --name 'name'
            cand --validator-key-file 'validator-key-file'
            cand --worker-key-file 'worker-key-file'
            cand --account-key-file 'account-key-file'
            cand --network-key-file 'network-key-file'
            cand --network-address 'network-address'
            cand --p2p-address 'p2p-address'
            cand --narwhal-primary-address 'narwhal-primary-address'
            cand --narwhal-worker-address 'narwhal-worker-address'
            cand --description 'description'
            cand --image-url 'image-url'
            cand --project-url 'project-url'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;list-validators'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;build-unsigned-checkpoint'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;examine-genesis-checkpoint'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;verify-and-sign'= {
            cand --key-file 'key-file'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;finalize'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;genesis-ceremony;help'= {
            cand init 'init'
            cand validate-state 'validate-state'
            cand add-validator 'add-validator'
            cand list-validators 'list-validators'
            cand build-unsigned-checkpoint 'build-unsigned-checkpoint'
            cand examine-genesis-checkpoint 'examine-genesis-checkpoint'
            cand verify-and-sign 'verify-and-sign'
            cand finalize 'finalize'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;genesis-ceremony;help;init'= {
        }
        &'sui;genesis-ceremony;help;validate-state'= {
        }
        &'sui;genesis-ceremony;help;add-validator'= {
        }
        &'sui;genesis-ceremony;help;list-validators'= {
        }
        &'sui;genesis-ceremony;help;build-unsigned-checkpoint'= {
        }
        &'sui;genesis-ceremony;help;examine-genesis-checkpoint'= {
        }
        &'sui;genesis-ceremony;help;verify-and-sign'= {
        }
        &'sui;genesis-ceremony;help;finalize'= {
        }
        &'sui;genesis-ceremony;help;help'= {
        }
        &'sui;keytool'= {
            cand --keystore-path 'keystore-path'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
            cand update-alias 'Update an old alias to a new one. If a new alias is not provided, a random one will be generated'
            cand convert 'Convert private key in Hex or Base64 to new format (Bech32 encoded 33 byte flag || private key starting with "suiprivkey"). Hex private key format import and export are both deprecated in Sui Wallet and Sui CLI Keystore. Use `sui keytool import` if you wish to import a key to Sui Keystore'
            cand decode-or-verify-tx 'Given a Base64 encoded transaction bytes, decode its components. If a signature is provided, verify the signature against the transaction and output the result'
            cand decode-multi-sig 'Given a Base64 encoded MultiSig signature, decode its components. If tx_bytes is passed in, verify the multisig'
            cand generate 'Generate a new keypair with key scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand import 'Add a new key to Sui CLI Keystore using either the input mnemonic phrase or a Bech32 encoded 33-byte `flag || privkey` starting with "suiprivkey", the key scheme flag {ed25519 | secp256k1 | secp256r1} and an optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Supports mnemonic phrase of word length 12, 15, 18, 21, 24. Set an alias for the key with the --alias flag. If no alias is provided, the tool will automatically generate one'
            cand export 'Output the private key of the given key identity in Sui CLI Keystore as Bech32 encoded string starting with `suiprivkey`'
            cand list 'List all keys by its Sui address, Base64 encoded public key, key scheme name in sui.keystore'
            cand load-keypair 'This reads the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). This prints out the account keypair as Base64 encoded `flag || privkey`, the network keypair, worker keypair, protocol keypair as Base64 encoded `privkey`'
            cand multi-sig-address 'To MultiSig Sui Address. Pass in a list of all public keys `flag || pk` in Base64. See `keytool list` for example public keys'
            cand multi-sig-combine-partial-sig 'Provides a list of participating signatures (`flag || sig || pk` encoded in Base64), threshold, a list of all public keys and a list of their weights that define the MultiSig address. Returns a valid MultiSig signature and its sender address. The result can be used as signature field for `sui client execute-signed-tx`. The sum of weights of all signatures must be >= the threshold'
            cand multi-sig-combine-partial-sig-legacy 'multi-sig-combine-partial-sig-legacy'
            cand show 'Read the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). It prints its Base64 encoded public key and the key scheme flag'
            cand sign 'Create signature using the private key for the given address (or its alias) in sui keystore. Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand sign-kms 'Creates a signature by leveraging AWS KMS. Pass in a key-id to leverage Amazon KMS to sign a message and the base64 pubkey. Generate PubKey from pem using MystenLabs/base64pemkey Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand unpack 'This takes [enum SuiKeyPair] of Base64 encoded of 33-byte `flag || privkey`). It outputs the keypair into a file at the current directory where the address is the filename, and prints out its Sui address, Base64 encoded public key, the key scheme, and the key scheme flag'
            cand zk-login-sign-and-execute-tx 'Given the max_epoch, generate an OAuth url, ask user to paste the redirect with id_token, call salt server, then call the prover server, create a test transaction, use the ephemeral key to sign and execute it by assembling to a serialized zkLogin signature'
            cand zk-login-enter-token 'A workaround to the above command because sometimes token pasting does not work (for Facebook). All the inputs required here are printed from the command above'
            cand zk-login-sig-verify 'Given a zkLogin signature, parse it if valid. If `bytes` provided, parse it as either as TransactionData or PersonalMessage based on `intent_scope`. It verifies the zkLogin signature based its latest JWK fetched. Example request: sui keytool zk-login-sig-verify --sig $SERIALIZED_ZKLOGIN_SIG --bytes $BYTES --intent-scope 0 --network devnet --curr-epoch 10'
            cand zk-login-insecure-sign-personal-message 'TESTING ONLY: Generate a fixed ephemeral key and its JWT token with test issuer. Produce a zklogin signature for the given data and max epoch. e.g. sui keytool zk-login-insecure-sign-personal-message --data "hello" --max-epoch 5'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;keytool;update-alias'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;convert'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;decode-or-verify-tx'= {
            cand --tx-bytes 'tx-bytes'
            cand --sig 'sig'
            cand --cur-epoch 'cur-epoch'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;decode-multi-sig'= {
            cand --multisig 'multisig'
            cand --tx-bytes 'tx-bytes'
            cand --cur-epoch 'cur-epoch'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;generate'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;keytool;import'= {
            cand --alias 'Sets an alias for this address. The alias must start with a letter and can contain only letters, digits, hyphens (-), or underscores (_)'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;export'= {
            cand --key-identity 'key-identity'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;list'= {
            cand -s 'Sort by alias'
            cand --sort-by-alias 'Sort by alias'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;load-keypair'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;multi-sig-address'= {
            cand --threshold 'threshold'
            cand --pks 'pks'
            cand --weights 'weights'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;multi-sig-combine-partial-sig'= {
            cand --sigs 'sigs'
            cand --pks 'pks'
            cand --weights 'weights'
            cand --threshold 'threshold'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;keytool;multi-sig-combine-partial-sig-legacy'= {
            cand --sigs 'sigs'
            cand --pks 'pks'
            cand --weights 'weights'
            cand --threshold 'threshold'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;show'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;sign'= {
            cand --address 'address'
            cand --data 'data'
            cand --intent 'intent'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;sign-kms'= {
            cand --data 'data'
            cand --keyid 'keyid'
            cand --intent 'intent'
            cand --base64pk 'base64pk'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;unpack'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;zk-login-sign-and-execute-tx'= {
            cand --max-epoch 'max-epoch'
            cand --network 'network'
            cand --fixed 'fixed'
            cand --test-multisig 'test-multisig'
            cand --sign-with-sk 'sign-with-sk'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;zk-login-enter-token'= {
            cand --parsed-token 'parsed-token'
            cand --max-epoch 'max-epoch'
            cand --jwt-randomness 'jwt-randomness'
            cand --kp-bigint 'kp-bigint'
            cand --ephemeral-key-identifier 'ephemeral-key-identifier'
            cand --network 'network'
            cand --test-multisig 'test-multisig'
            cand --sign-with-sk 'sign-with-sk'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;zk-login-sig-verify'= {
            cand --sig 'The Base64 of the serialized zkLogin signature'
            cand --bytes 'The Base64 of the BCS encoded TransactionData or PersonalMessage'
            cand --intent-scope 'Either 0 for TransactionData or 3 for PersonalMessage'
            cand --cur-epoch 'The current epoch for the network to verify the signature''s max_epoch against'
            cand --network 'The network to verify the signature for, determines ZkLoginEnv'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;zk-login-insecure-sign-personal-message'= {
            cand --data 'The base64 encoded string of the message to sign, without the intent message wrapping'
            cand --max-epoch 'The max epoch used for the zklogin signature validity'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;keytool;help'= {
            cand update-alias 'Update an old alias to a new one. If a new alias is not provided, a random one will be generated'
            cand convert 'Convert private key in Hex or Base64 to new format (Bech32 encoded 33 byte flag || private key starting with "suiprivkey"). Hex private key format import and export are both deprecated in Sui Wallet and Sui CLI Keystore. Use `sui keytool import` if you wish to import a key to Sui Keystore'
            cand decode-or-verify-tx 'Given a Base64 encoded transaction bytes, decode its components. If a signature is provided, verify the signature against the transaction and output the result'
            cand decode-multi-sig 'Given a Base64 encoded MultiSig signature, decode its components. If tx_bytes is passed in, verify the multisig'
            cand generate 'Generate a new keypair with key scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand import 'Add a new key to Sui CLI Keystore using either the input mnemonic phrase or a Bech32 encoded 33-byte `flag || privkey` starting with "suiprivkey", the key scheme flag {ed25519 | secp256k1 | secp256r1} and an optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Supports mnemonic phrase of word length 12, 15, 18, 21, 24. Set an alias for the key with the --alias flag. If no alias is provided, the tool will automatically generate one'
            cand export 'Output the private key of the given key identity in Sui CLI Keystore as Bech32 encoded string starting with `suiprivkey`'
            cand list 'List all keys by its Sui address, Base64 encoded public key, key scheme name in sui.keystore'
            cand load-keypair 'This reads the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). This prints out the account keypair as Base64 encoded `flag || privkey`, the network keypair, worker keypair, protocol keypair as Base64 encoded `privkey`'
            cand multi-sig-address 'To MultiSig Sui Address. Pass in a list of all public keys `flag || pk` in Base64. See `keytool list` for example public keys'
            cand multi-sig-combine-partial-sig 'Provides a list of participating signatures (`flag || sig || pk` encoded in Base64), threshold, a list of all public keys and a list of their weights that define the MultiSig address. Returns a valid MultiSig signature and its sender address. The result can be used as signature field for `sui client execute-signed-tx`. The sum of weights of all signatures must be >= the threshold'
            cand multi-sig-combine-partial-sig-legacy 'multi-sig-combine-partial-sig-legacy'
            cand show 'Read the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). It prints its Base64 encoded public key and the key scheme flag'
            cand sign 'Create signature using the private key for the given address (or its alias) in sui keystore. Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand sign-kms 'Creates a signature by leveraging AWS KMS. Pass in a key-id to leverage Amazon KMS to sign a message and the base64 pubkey. Generate PubKey from pem using MystenLabs/base64pemkey Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand unpack 'This takes [enum SuiKeyPair] of Base64 encoded of 33-byte `flag || privkey`). It outputs the keypair into a file at the current directory where the address is the filename, and prints out its Sui address, Base64 encoded public key, the key scheme, and the key scheme flag'
            cand zk-login-sign-and-execute-tx 'Given the max_epoch, generate an OAuth url, ask user to paste the redirect with id_token, call salt server, then call the prover server, create a test transaction, use the ephemeral key to sign and execute it by assembling to a serialized zkLogin signature'
            cand zk-login-enter-token 'A workaround to the above command because sometimes token pasting does not work (for Facebook). All the inputs required here are printed from the command above'
            cand zk-login-sig-verify 'Given a zkLogin signature, parse it if valid. If `bytes` provided, parse it as either as TransactionData or PersonalMessage based on `intent_scope`. It verifies the zkLogin signature based its latest JWK fetched. Example request: sui keytool zk-login-sig-verify --sig $SERIALIZED_ZKLOGIN_SIG --bytes $BYTES --intent-scope 0 --network devnet --curr-epoch 10'
            cand zk-login-insecure-sign-personal-message 'TESTING ONLY: Generate a fixed ephemeral key and its JWT token with test issuer. Produce a zklogin signature for the given data and max epoch. e.g. sui keytool zk-login-insecure-sign-personal-message --data "hello" --max-epoch 5'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;keytool;help;update-alias'= {
        }
        &'sui;keytool;help;convert'= {
        }
        &'sui;keytool;help;decode-or-verify-tx'= {
        }
        &'sui;keytool;help;decode-multi-sig'= {
        }
        &'sui;keytool;help;generate'= {
        }
        &'sui;keytool;help;import'= {
        }
        &'sui;keytool;help;export'= {
        }
        &'sui;keytool;help;list'= {
        }
        &'sui;keytool;help;load-keypair'= {
        }
        &'sui;keytool;help;multi-sig-address'= {
        }
        &'sui;keytool;help;multi-sig-combine-partial-sig'= {
        }
        &'sui;keytool;help;multi-sig-combine-partial-sig-legacy'= {
        }
        &'sui;keytool;help;show'= {
        }
        &'sui;keytool;help;sign'= {
        }
        &'sui;keytool;help;sign-kms'= {
        }
        &'sui;keytool;help;unpack'= {
        }
        &'sui;keytool;help;zk-login-sign-and-execute-tx'= {
        }
        &'sui;keytool;help;zk-login-enter-token'= {
        }
        &'sui;keytool;help;zk-login-sig-verify'= {
        }
        &'sui;keytool;help;zk-login-insecure-sign-personal-message'= {
        }
        &'sui;keytool;help;help'= {
        }
        &'sui;client'= {
            cand --client.config 'Sets the file storing the state of our user accounts (an empty one will be created if missing)'
            cand --client.env 'The Sui environment to use. This must be present in the current config file'
            cand --json 'Return command outputs in json format'
            cand -y 'y'
            cand --yes 'yes'
            cand -h 'Print help'
            cand --help 'Print help'
            cand active-address 'Default address used for commands when none specified'
            cand active-env 'Default environment used for commands when none specified'
            cand addresses 'Obtain the Addresses managed by the client'
            cand balance 'List the coin balance of an address'
            cand call 'Call Move function'
            cand chain-identifier 'Query the chain identifier from the rpc endpoint'
            cand dynamic-field 'Query a dynamic field by its address'
            cand envs 'List all Sui environments'
            cand execute-signed-tx 'Execute a Signed Transaction. This is useful when the user prefers to sign elsewhere and use this command to execute'
            cand execute-combined-signed-tx 'Execute a combined serialized SenderSignedData string'
            cand faucet 'Request gas coin from faucet. By default, it will use the active address and the active network'
            cand gas 'Obtain all gas objects owned by the address. An address'' alias can be used instead of the address'
            cand merge-coin 'Merge two coin objects into one coin'
            cand new-address 'Generate new address and keypair with keypair scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand new-env 'Add new Sui environment'
            cand object 'Get object info'
            cand objects 'Obtain all objects owned by the address. It also accepts an address by its alias'
            cand party-transfer 'Transfer object to party ownership'
            cand pay 'Pay coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts'
            cand pay-all-sui 'Pay all residual SUI coins to the recipient with input coins, after deducting the gas cost. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand pay-sui 'Pay SUI coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand ptb 'Run a PTB from the provided args'
            cand publish 'Publish Move modules'
            cand serialized-tx 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction'
            cand serialized-tx-kind 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction kind'
            cand split-coin 'Split a coin object into multiple coins'
            cand switch 'Switch active address and network(e.g., devnet, local rpc server)'
            cand tx-block 'Get the effects of executing the given transaction block'
            cand transfer 'Transfer object'
            cand transfer-sui 'Transfer SUI, and pay gas with the same SUI coin object. If amount is specified, only the amount is transferred; otherwise the entire object is transferred'
            cand upgrade 'Upgrade Move modules'
            cand verify-bytecode-meter 'Run the bytecode verifier on the package'
            cand verify-source 'Verify local Move packages against on-chain packages, and optionally their dependencies'
            cand remove-address 'Remove an existing address by its alias or hexadecimal string'
            cand replay-transaction 'Replay a given transaction to view transaction effects (deprecated; use `sui replay` instead)'
            cand replay-batch 'Replay transactions listed in a file (deprecated; use `sui replay` instead)'
            cand replay-checkpoint 'Replay all transactions in a range of checkpoints (deprecated; use `sui replay` instead)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;client;active-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;active-env'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;addresses'= {
            cand -s 'Sort by alias instead of address'
            cand --sort-by-alias 'Sort by alias instead of address'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;balance'= {
            cand --coin-type 'Show balance for the specified coin (e.g., 0x2::sui::SUI). All coins will be shown if none is passed'
            cand --with-coins 'Show a list with each coin''s object ID and balance'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;call'= {
            cand --package 'Object ID of the package, which contains the module'
            cand --module 'The name of the module in the package'
            cand --function 'Function name in module'
            cand --type-args 'Type arguments to the generic function being called. All must be specified, or the call will fail'
            cand --args 'Simplified ordered args like in the function syntax ObjectIDs, Addresses must be hex strings'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;chain-identifier'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;dynamic-field'= {
            cand --cursor 'Optional paging cursor'
            cand --limit 'Maximum item returned per page'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;envs'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;execute-signed-tx'= {
            cand --tx-bytes 'BCS serialized transaction data bytes without its type tag, as base64 encoded string. This is the output of sui client command using --serialize-unsigned-transaction'
            cand --signatures 'A list of Base64 encoded signatures `flag || signature || pubkey`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;execute-combined-signed-tx'= {
            cand --signed-tx-bytes 'BCS serialized sender signed data, as base64 encoded string. This is the output of sui client command using --serialize-signed-transaction'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;faucet'= {
            cand --address 'Address (or its alias)'
            cand --url 'The url to the faucet'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;gas'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;merge-coin'= {
            cand --primary-coin 'The address of the coin to merge into'
            cand --coin-to-merge 'The address of the coin to be merged'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;new-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;new-env'= {
            cand --alias 'alias'
            cand --rpc 'rpc'
            cand --ws 'ws'
            cand --basic-auth 'Basic auth in the format of username:password'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;object'= {
            cand --bcs 'Return the bcs serialized version of the object'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;objects'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;party-transfer'= {
            cand --to 'Recipient address (or its alias if it''s an address in the keystore)'
            cand --object-id 'ID of the object to transfer'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;pay'= {
            cand --input-coins 'The input coins to be used for pay recipients, following the specified amounts'
            cand --recipients 'The recipient addresses, must be of same length as amounts. Aliases of addresses are also accepted as input'
            cand --amounts 'The amounts to be paid, following the order of recipients'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;pay-all-sui'= {
            cand --input-coins 'The input coins to be used for pay recipients, including the gas coin'
            cand --recipient 'The recipient address (or its alias if it''s an address in the keystore)'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;pay-sui'= {
            cand --input-coins 'The input coins to be used for pay recipients, including the gas coin'
            cand --recipients 'The recipient addresses, must be of same length as amounts. Aliases of addresses are also accepted as input'
            cand --amounts 'The amounts to be paid, following the order of recipients'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;ptb'= {
            cand --json 'Return command outputs in json format'
        }
        &'sui;client;publish'= {
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand --skip-dependency-verification 'Publish the package without checking whether dependency source code compiles to the on-chain bytecode'
            cand --verify-deps 'Check that the dependency source code compiles to the on-chain bytecode before publishing the package (currently the default behavior)'
            cand --with-unpublished-dependencies 'Also publish transitive dependencies that have not already been published'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;serialized-tx'= {
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;serialized-tx-kind'= {
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;split-coin'= {
            cand --coin-id 'ID of the coin object to split'
            cand --amounts 'Specific amounts to split out from the coin'
            cand --count 'Count of equal-size coins to split into'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;switch'= {
            cand --address 'An address to be used as the active address for subsequent commands. It accepts also the alias of the address'
            cand --env 'The RPC server URL (e.g., local rpc server, devnet rpc server, etc) to be used for subsequent commands'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;tx-block'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;transfer'= {
            cand --to 'Recipient address (or its alias if it''s an address in the keystore)'
            cand --object-id 'ID of the object to transfer'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;transfer-sui'= {
            cand --to 'Recipient address (or its alias if it''s an address in the keystore)'
            cand --sui-coin-object-id 'ID of the coin to transfer. This is also the gas object'
            cand --amount 'The amount to transfer, if not specified, the entire coin object will be transferred'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;upgrade'= {
            cand -c 'ID of the upgrade capability for the package being upgraded'
            cand --upgrade-capability 'ID of the upgrade capability for the package being upgraded'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --gas 'IDs of gas objects to be used for gas payment. If none are provided, coins are selected automatically to cover the gas budget'
            cand --gas-budget 'An optional gas budget for this transaction (in MIST). If gas budget is not provided, the tool will first perform a dry run to estimate the gas cost, and then it will execute the transaction. Please note that this incurs a small cost in performance due to the additional dry run call'
            cand --gas-price 'An optional gas price for this transaction (in MIST). If gas price is not provided, the tool will use the current reference gas price from RPC'
            cand --gas-sponsor 'An optional field to specify a gas sponsor address. If provided, the gas owner is set to this address, rather than the transaction''s sender'
            cand --sender 'Set the transaction sender to this address. When not specified, the sender is inferred by finding the owner of the gas payment. Note that when setting this field, the transaction will fail to execute if the sender''s private key is not in the keystore; similarly, it will fail when using this with `--serialize-signed-transaction` flag if the private key corresponding to this address is not in keystore'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand --verify-compatibility 'Verify package compatibility locally before publishing'
            cand --skip-dependency-verification 'Upgrade the package without checking whether dependency source code compiles to the on-chain bytecode'
            cand --verify-deps 'Check that the dependency source code compiles to the on-chain bytecode before upgrading the package (currently the default behavior)'
            cand --with-unpublished-dependencies 'Also publish transitive dependencies that have not already been published'
            cand --tx-digest 'Compute the transaction digest and print it out, but do not execute the transaction'
            cand --dry-run 'Perform a dry run of the transaction, without executing it'
            cand --dev-inspect 'Perform a dev inspect'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --serialize-signed-transaction 'Instead of executing the transaction, serialize the bcs bytes of the signed transaction data (SenderSignedData) using base64 encoding, and print out the string <SIGNED_TX_BYTES>. The string can be used to execute transaction with `sui client execute-combined-signed-tx --signed-tx-bytes <SIGNED_TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'sui;client;verify-bytecode-meter'= {
            cand --package 'Path to directory containing a Move package, (defaults to the current directory)'
            cand --protocol-version 'Protocol version to use for the bytecode verifier (defaults to the latest protocol version)'
            cand --module 'Paths to specific pre-compiled module bytecode to verify (instead of an entire package). Multiple modules can be verified by passing multiple --module flags. They will be treated as if they were one package (subject to the overall package limit)'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;verify-source'= {
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --address-override 'If specified, override the addresses for the package''s own modules with this address. Only works for unpublished modules (whose addresses are currently 0x0)'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand --verify-deps 'Verify on-chain dependencies'
            cand --skip-source 'Don''t verify source (only valid if --verify-deps is enabled)'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;remove-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;replay-transaction'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;replay-batch'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;replay-checkpoint'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;client;help'= {
            cand active-address 'Default address used for commands when none specified'
            cand active-env 'Default environment used for commands when none specified'
            cand addresses 'Obtain the Addresses managed by the client'
            cand balance 'List the coin balance of an address'
            cand call 'Call Move function'
            cand chain-identifier 'Query the chain identifier from the rpc endpoint'
            cand dynamic-field 'Query a dynamic field by its address'
            cand envs 'List all Sui environments'
            cand execute-signed-tx 'Execute a Signed Transaction. This is useful when the user prefers to sign elsewhere and use this command to execute'
            cand execute-combined-signed-tx 'Execute a combined serialized SenderSignedData string'
            cand faucet 'Request gas coin from faucet. By default, it will use the active address and the active network'
            cand gas 'Obtain all gas objects owned by the address. An address'' alias can be used instead of the address'
            cand merge-coin 'Merge two coin objects into one coin'
            cand new-address 'Generate new address and keypair with keypair scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand new-env 'Add new Sui environment'
            cand object 'Get object info'
            cand objects 'Obtain all objects owned by the address. It also accepts an address by its alias'
            cand party-transfer 'Transfer object to party ownership'
            cand pay 'Pay coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts'
            cand pay-all-sui 'Pay all residual SUI coins to the recipient with input coins, after deducting the gas cost. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand pay-sui 'Pay SUI coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand ptb 'Run a PTB from the provided args'
            cand publish 'Publish Move modules'
            cand serialized-tx 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction'
            cand serialized-tx-kind 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction kind'
            cand split-coin 'Split a coin object into multiple coins'
            cand switch 'Switch active address and network(e.g., devnet, local rpc server)'
            cand tx-block 'Get the effects of executing the given transaction block'
            cand transfer 'Transfer object'
            cand transfer-sui 'Transfer SUI, and pay gas with the same SUI coin object. If amount is specified, only the amount is transferred; otherwise the entire object is transferred'
            cand upgrade 'Upgrade Move modules'
            cand verify-bytecode-meter 'Run the bytecode verifier on the package'
            cand verify-source 'Verify local Move packages against on-chain packages, and optionally their dependencies'
            cand remove-address 'Remove an existing address by its alias or hexadecimal string'
            cand replay-transaction 'Replay a given transaction to view transaction effects (deprecated; use `sui replay` instead)'
            cand replay-batch 'Replay transactions listed in a file (deprecated; use `sui replay` instead)'
            cand replay-checkpoint 'Replay all transactions in a range of checkpoints (deprecated; use `sui replay` instead)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;client;help;active-address'= {
        }
        &'sui;client;help;active-env'= {
        }
        &'sui;client;help;addresses'= {
        }
        &'sui;client;help;balance'= {
        }
        &'sui;client;help;call'= {
        }
        &'sui;client;help;chain-identifier'= {
        }
        &'sui;client;help;dynamic-field'= {
        }
        &'sui;client;help;envs'= {
        }
        &'sui;client;help;execute-signed-tx'= {
        }
        &'sui;client;help;execute-combined-signed-tx'= {
        }
        &'sui;client;help;faucet'= {
        }
        &'sui;client;help;gas'= {
        }
        &'sui;client;help;merge-coin'= {
        }
        &'sui;client;help;new-address'= {
        }
        &'sui;client;help;new-env'= {
        }
        &'sui;client;help;object'= {
        }
        &'sui;client;help;objects'= {
        }
        &'sui;client;help;party-transfer'= {
        }
        &'sui;client;help;pay'= {
        }
        &'sui;client;help;pay-all-sui'= {
        }
        &'sui;client;help;pay-sui'= {
        }
        &'sui;client;help;ptb'= {
        }
        &'sui;client;help;publish'= {
        }
        &'sui;client;help;serialized-tx'= {
        }
        &'sui;client;help;serialized-tx-kind'= {
        }
        &'sui;client;help;split-coin'= {
        }
        &'sui;client;help;switch'= {
        }
        &'sui;client;help;tx-block'= {
        }
        &'sui;client;help;transfer'= {
        }
        &'sui;client;help;transfer-sui'= {
        }
        &'sui;client;help;upgrade'= {
        }
        &'sui;client;help;verify-bytecode-meter'= {
        }
        &'sui;client;help;verify-source'= {
        }
        &'sui;client;help;remove-address'= {
        }
        &'sui;client;help;replay-transaction'= {
        }
        &'sui;client;help;replay-batch'= {
        }
        &'sui;client;help;replay-checkpoint'= {
        }
        &'sui;client;help;help'= {
        }
        &'sui;validator'= {
            cand --client.config 'Sets the file storing the state of our user accounts (an empty one will be created if missing)'
            cand --json 'Return command outputs in json format'
            cand -y 'y'
            cand --yes 'yes'
            cand -h 'Print help'
            cand --help 'Print help'
            cand make-validator-info 'make-validator-info'
            cand become-candidate 'Arguments related to transaction processing'
            cand join-committee 'Arguments related to transaction processing'
            cand leave-committee 'Arguments related to transaction processing'
            cand display-metadata 'display-metadata'
            cand update-metadata 'Arguments related to transaction processing'
            cand update-gas-price 'Update gas price that is used to calculate Reference Gas Price'
            cand report-validator 'Report or un-report a validator. Report or un-report a validator'
            cand serialize-payload-pop 'Serialize the payload that is used to generate Proof of Possession. This is useful to take the payload offline for an Authority protocol keypair to sign'
            cand display-gas-price-update-raw-txn 'Print out the serialized data of a transaction that sets the gas price quote for a validator'
            cand register-bridge-committee 'Sui native bridge committee member registration'
            cand update-bridge-committee-node-url 'Update sui native bridge committee node url'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;validator;make-validator-info'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;become-candidate'= {
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;join-committee'= {
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;leave-committee'= {
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;display-metadata'= {
            cand --json 'json'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata'= {
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
            cand name 'Update name. Effectuate immediately'
            cand description 'Update description. Effectuate immediately'
            cand image-url 'Update Image URL. Effectuate immediately'
            cand project-url 'Update Project URL. Effectuate immediately'
            cand network-address 'Update Network Address. Effectuate from next epoch'
            cand primary-address 'Update Primary Address. Effectuate from next epoch'
            cand worker-address 'Update Worker Address. Effectuate from next epoch'
            cand p2p-address 'Update P2P Address. Effectuate from next epoch'
            cand network-pub-key 'Update Network Public Key. Effectuate from next epoch'
            cand worker-pub-key 'Update Worker Public Key. Effectuate from next epoch'
            cand protocol-pub-key 'Update Protocol Public Key and Proof and Possession. Effectuate from next epoch'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;validator;update-metadata;name'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;description'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;image-url'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;project-url'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;network-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;primary-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;worker-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;p2p-address'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;network-pub-key'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;worker-pub-key'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;protocol-pub-key'= {
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-metadata;help'= {
            cand name 'Update name. Effectuate immediately'
            cand description 'Update description. Effectuate immediately'
            cand image-url 'Update Image URL. Effectuate immediately'
            cand project-url 'Update Project URL. Effectuate immediately'
            cand network-address 'Update Network Address. Effectuate from next epoch'
            cand primary-address 'Update Primary Address. Effectuate from next epoch'
            cand worker-address 'Update Worker Address. Effectuate from next epoch'
            cand p2p-address 'Update P2P Address. Effectuate from next epoch'
            cand network-pub-key 'Update Network Public Key. Effectuate from next epoch'
            cand worker-pub-key 'Update Worker Public Key. Effectuate from next epoch'
            cand protocol-pub-key 'Update Protocol Public Key and Proof and Possession. Effectuate from next epoch'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;validator;update-metadata;help;name'= {
        }
        &'sui;validator;update-metadata;help;description'= {
        }
        &'sui;validator;update-metadata;help;image-url'= {
        }
        &'sui;validator;update-metadata;help;project-url'= {
        }
        &'sui;validator;update-metadata;help;network-address'= {
        }
        &'sui;validator;update-metadata;help;primary-address'= {
        }
        &'sui;validator;update-metadata;help;worker-address'= {
        }
        &'sui;validator;update-metadata;help;p2p-address'= {
        }
        &'sui;validator;update-metadata;help;network-pub-key'= {
        }
        &'sui;validator;update-metadata;help;worker-pub-key'= {
        }
        &'sui;validator;update-metadata;help;protocol-pub-key'= {
        }
        &'sui;validator;update-metadata;help;help'= {
        }
        &'sui;validator;update-gas-price'= {
            cand --operation-cap-id 'Optional when sender is the validator itself and it holds the Cap object. Required when sender is not the validator itself. Validator''s OperationCap ID can be found by using the `display-metadata` subcommand'
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;report-validator'= {
            cand --operation-cap-id 'Optional when sender is reporter validator itself and it holds the Cap object. Required when sender is not the reporter validator itself. Validator''s OperationCap ID can be found by using the `display-metadata` subcommand'
            cand --undo-report 'If true, undo an existing report'
            cand --gas-budget 'Gas budget for this transaction'
            cand --serialize-unsigned-transaction 'Instead of executing the transaction, serialize the bcs bytes of the unsigned transaction data (TransactionData) using base64 encoding, and print out the string <TX_BYTES>. The string can be used to execute transaction with `sui client execute-signed-tx --tx-bytes <TX_BYTES>`'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;serialize-payload-pop'= {
            cand --account-address 'Authority account address encoded in hex with 0x prefix'
            cand --protocol-public-key 'Authority protocol public key encoded in hex'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;display-gas-price-update-raw-txn'= {
            cand --sender-address 'Address of the transaction sender'
            cand --operation-cap-id 'Object ID of a validator''s OperationCap, used for setting gas price and reportng validators'
            cand --new-gas-price 'Gas price to be set to'
            cand --gas-budget 'Gas budget for this transaction'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;register-bridge-committee'= {
            cand --bridge-authority-key-path 'Path to Bridge Authority Key file'
            cand --bridge-authority-url 'Bridge authority URL which clients collects action signatures from'
            cand --validator-address 'Must present if `print_unsigned_transaction_only` is true'
            cand --gas-budget 'Gas budget for this transaction'
            cand --print-only 'If true, only print the unsigned transaction and do not execute it. This is useful for offline signing'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;update-bridge-committee-node-url'= {
            cand --bridge-authority-url 'New node url to be registered in the on chain bridge object'
            cand --validator-address 'Must be present if `print_unsigned_transaction_only` is true'
            cand --gas-budget 'Gas budget for this transaction'
            cand --print-only 'If true, only print the unsigned transaction and do not execute it. This is useful for offline signing'
            cand --json 'Return command outputs in json format'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;validator;help'= {
            cand make-validator-info 'make-validator-info'
            cand become-candidate 'Arguments related to transaction processing'
            cand join-committee 'Arguments related to transaction processing'
            cand leave-committee 'Arguments related to transaction processing'
            cand display-metadata 'display-metadata'
            cand update-metadata 'Arguments related to transaction processing'
            cand update-gas-price 'Update gas price that is used to calculate Reference Gas Price'
            cand report-validator 'Report or un-report a validator. Report or un-report a validator'
            cand serialize-payload-pop 'Serialize the payload that is used to generate Proof of Possession. This is useful to take the payload offline for an Authority protocol keypair to sign'
            cand display-gas-price-update-raw-txn 'Print out the serialized data of a transaction that sets the gas price quote for a validator'
            cand register-bridge-committee 'Sui native bridge committee member registration'
            cand update-bridge-committee-node-url 'Update sui native bridge committee node url'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;validator;help;make-validator-info'= {
        }
        &'sui;validator;help;become-candidate'= {
        }
        &'sui;validator;help;join-committee'= {
        }
        &'sui;validator;help;leave-committee'= {
        }
        &'sui;validator;help;display-metadata'= {
        }
        &'sui;validator;help;update-metadata'= {
            cand name 'Update name. Effectuate immediately'
            cand description 'Update description. Effectuate immediately'
            cand image-url 'Update Image URL. Effectuate immediately'
            cand project-url 'Update Project URL. Effectuate immediately'
            cand network-address 'Update Network Address. Effectuate from next epoch'
            cand primary-address 'Update Primary Address. Effectuate from next epoch'
            cand worker-address 'Update Worker Address. Effectuate from next epoch'
            cand p2p-address 'Update P2P Address. Effectuate from next epoch'
            cand network-pub-key 'Update Network Public Key. Effectuate from next epoch'
            cand worker-pub-key 'Update Worker Public Key. Effectuate from next epoch'
            cand protocol-pub-key 'Update Protocol Public Key and Proof and Possession. Effectuate from next epoch'
        }
        &'sui;validator;help;update-metadata;name'= {
        }
        &'sui;validator;help;update-metadata;description'= {
        }
        &'sui;validator;help;update-metadata;image-url'= {
        }
        &'sui;validator;help;update-metadata;project-url'= {
        }
        &'sui;validator;help;update-metadata;network-address'= {
        }
        &'sui;validator;help;update-metadata;primary-address'= {
        }
        &'sui;validator;help;update-metadata;worker-address'= {
        }
        &'sui;validator;help;update-metadata;p2p-address'= {
        }
        &'sui;validator;help;update-metadata;network-pub-key'= {
        }
        &'sui;validator;help;update-metadata;worker-pub-key'= {
        }
        &'sui;validator;help;update-metadata;protocol-pub-key'= {
        }
        &'sui;validator;help;update-gas-price'= {
        }
        &'sui;validator;help;report-validator'= {
        }
        &'sui;validator;help;serialize-payload-pop'= {
        }
        &'sui;validator;help;display-gas-price-update-raw-txn'= {
        }
        &'sui;validator;help;register-bridge-committee'= {
        }
        &'sui;validator;help;update-bridge-committee-node-url'= {
        }
        &'sui;validator;help;help'= {
        }
        &'sui;move'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --client.config 'Sets the file storing the state of our user accounts (an empty one will be created if missing)'
            cand --client.env 'The Sui environment to use. This must be present in the current config file'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
            cand build 'build'
            cand coverage 'Inspect test coverage for this package. A previous test run with the `--coverage` flag must have previously been run'
            cand disassemble 'disassemble'
            cand manage-package 'Record addresses (Object IDs) for where this package is published on chain (this command sets variables in Move.lock)'
            cand migrate 'Migrate to Move 2024 for the package at `path`. If no path is provided defaults to current directory'
            cand new 'Create a new Move package with name `name` at `path`. If `path` is not provided the package will be created in the directory `name`'
            cand test 'Run Move unit tests in this package'
            cand summary 'Generate a serialized summary of a Move package (e.g., functions, structs, annotations, etc.)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;move;build'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --with-unpublished-dependencies 'Include the contents of packages in dependencies that haven''t been published (only relevant when dumping bytecode as base64)'
            cand --dump-bytecode-as-base64 'Whether we are printing in base64'
            cand --ignore-chain '[Mainly for testing, not recommended for production] Don''t specialize the package to the active chain when dumping bytecode as Base64. This allows building to proceed without a network connection or active environment, but it will not be able to automatically determine the addresses of its dependencies'
            cand --generate-struct-layouts 'If true, generate struct layout schemas for all struct types passed into `entry` functions declared by modules in this package These layout schemas can be consumed by clients (e.g., the TypeScript SDK) to enable serialization/deserialization of transaction arguments and events'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;coverage'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
            cand summary 'Display a coverage summary for all modules in this package'
            cand source 'Display coverage information about the module against source code'
            cand bytecode 'Display coverage information about the module against disassembled bytecode'
            cand lcov 'Generate LCOV coverage information for the package. Requires traces to be present. Run tests with `--trace` to generate traces'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;move;coverage;summary'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --summarize-functions 'Whether function coverage summaries should be displayed'
            cand --csv 'Output CSV data of coverage'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;coverage;source'= {
            cand --module 'module'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;coverage;bytecode'= {
            cand --module 'module'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;coverage;lcov'= {
            cand --differential-test 'Compute differential coverage for the provided test name. Lines that are hit by this test only will show as covered, and lines that are hit by both this test and all other tests will show as "uncovered". Otherwise lines are not annotated with coverage information'
            cand --only-test 'Compute coverage for the provided test name. Only this test will contribute to the coverage calculation'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;coverage;help'= {
            cand summary 'Display a coverage summary for all modules in this package'
            cand source 'Display coverage information about the module against source code'
            cand bytecode 'Display coverage information about the module against disassembled bytecode'
            cand lcov 'Generate LCOV coverage information for the package. Requires traces to be present. Run tests with `--trace` to generate traces'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;move;coverage;help;summary'= {
        }
        &'sui;move;coverage;help;source'= {
        }
        &'sui;move;coverage;help;bytecode'= {
        }
        &'sui;move;coverage;help;lcov'= {
        }
        &'sui;move;coverage;help;help'= {
        }
        &'sui;move;disassemble'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --Xdebug 'Whether to display the disassembly in raw Debug format'
            cand -i 'i'
            cand --interactive 'interactive'
            cand --bytecode-map 'Print the "bytecode map" (source map for disassembled bytecode)'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;manage-package'= {
            cand --environment 'The environment to associate this package information with (consider using `sui client active-env`)'
            cand --network-id 'The network chain identifer. Use ''35834a8a'' for mainnet'
            cand --original-id 'The original address (Object ID) where this package is published'
            cand --latest-id 'The most recent address (Object ID) where this package is published. It is the same as ''original-id'' if the package is immutable and published once. It is different from ''original-id'' if the package has been upgraded to a different address'
            cand --version-number 'The version number of the published package. It is ''1'' if the package is immutable and published once. It is some number greater than ''1'' if the package has been upgraded once or more'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;migrate'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;new'= {
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;test'= {
            cand -i 'Bound the amount of gas used by any one test'
            cand --gas-limit 'Bound the amount of gas used by any one test'
            cand -t 'Number of threads to use for running tests'
            cand --threads 'Number of threads to use for running tests'
            cand -s 'Report test statistics at the end of testing. CSV report generated if ''csv'' passed'
            cand --statistics 'Report test statistics at the end of testing. CSV report generated if ''csv'' passed'
            cand --seed 'The seed to use for the randomness generator'
            cand --rand-num-iters 'The number of iterations to run each test that uses generated values (only used with #[random_test])'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand -l 'List all tests'
            cand --list 'List all tests'
            cand --verbose 'Verbose mode'
            cand --coverage 'Collect coverage information for later use with the various `move coverage` subcommands. Currently supported only in debug builds'
            cand --trace 'Enable tracing for tests'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;summary'= {
            cand -o 'The output format the summary should be generated in'
            cand --output-format 'The output format the summary should be generated in'
            cand --output-directory 'Directory that all generated summaries should be nested under'
            cand --package-id 'The object ID to summarize if `package-id` is present. The `--path` will be ignored if this field is used'
            cand -p 'Path to a package which the command should be run with respect to'
            cand --path 'Path to a package which the command should be run with respect to'
            cand --install-dir 'Installation directory for compiled artifacts. Defaults to current directory'
            cand --default-move-flavor 'Default flavor for move compilation, if not specified in the package''s config'
            cand --default-move-edition 'Default edition for move compilation, if not specified in the package''s config'
            cand --mode 'Arbitrary mode -- this will be used to enable or filter user-defined `#[mode(<MODE>)]` annotations during compilation'
            cand --bytecode 'Whether we are generating a summary for a package or for a directory of bytecode modules. All `.mv` files under the path supplied (or current directory if none supplied) will be summarized'
            cand -d 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --dev 'Compile in ''dev'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used if this flag is set. This flag is useful for development of packages that expose named addresses that are not set to a specific value'
            cand --test 'Compile in ''test'' mode. The ''dev-addresses'' and ''dev-dependencies'' fields will be used along with any code in the ''tests'' directory'
            cand --doc 'Generate documentation for packages'
            cand --disassemble 'Save disassembly for generated bytecode along with bytecode maps (source maps for disassembled bytecode)'
            cand --force 'Force recompilation of all packages'
            cand --fetch-deps-only 'Only fetch dependency repos to MOVE_HOME'
            cand --skip-fetch-latest-git-deps 'Skip fetching latest git dependencies'
            cand --dependencies-are-root 'If set, dependency packages are treated as root packages. Notably, this will remove warning suppression in dependency packages'
            cand --silence-warnings 'If set, ignore any compiler warnings'
            cand --warnings-are-errors 'If set, warnings become errors'
            cand --json-errors 'If set, reports errors at JSON'
            cand --no-lint 'If `true`, disable linters'
            cand --lint 'If `true`, enables extra linters'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;move;help'= {
            cand build 'build'
            cand coverage 'Inspect test coverage for this package. A previous test run with the `--coverage` flag must have previously been run'
            cand disassemble 'disassemble'
            cand manage-package 'Record addresses (Object IDs) for where this package is published on chain (this command sets variables in Move.lock)'
            cand migrate 'Migrate to Move 2024 for the package at `path`. If no path is provided defaults to current directory'
            cand new 'Create a new Move package with name `name` at `path`. If `path` is not provided the package will be created in the directory `name`'
            cand test 'Run Move unit tests in this package'
            cand summary 'Generate a serialized summary of a Move package (e.g., functions, structs, annotations, etc.)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;move;help;build'= {
        }
        &'sui;move;help;coverage'= {
            cand summary 'Display a coverage summary for all modules in this package'
            cand source 'Display coverage information about the module against source code'
            cand bytecode 'Display coverage information about the module against disassembled bytecode'
            cand lcov 'Generate LCOV coverage information for the package. Requires traces to be present. Run tests with `--trace` to generate traces'
        }
        &'sui;move;help;coverage;summary'= {
        }
        &'sui;move;help;coverage;source'= {
        }
        &'sui;move;help;coverage;bytecode'= {
        }
        &'sui;move;help;coverage;lcov'= {
        }
        &'sui;move;help;disassemble'= {
        }
        &'sui;move;help;manage-package'= {
        }
        &'sui;move;help;migrate'= {
        }
        &'sui;move;help;new'= {
        }
        &'sui;move;help;test'= {
        }
        &'sui;move;help;summary'= {
        }
        &'sui;move;help;help'= {
        }
        &'sui;bridge-committee-init'= {
            cand --network.config 'network.config'
            cand --client.config 'client.config'
            cand --bridge_committee.config 'bridge_committee.config'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;fire-drill'= {
            cand -h 'Print help'
            cand --help 'Print help'
            cand metadata-rotation 'metadata-rotation'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;fire-drill;metadata-rotation'= {
            cand --sui-node-config-path 'Path to sui node config'
            cand --account-key-path 'Path to account key file'
            cand --fullnode-rpc-url 'Jsonrpc url for a reliable fullnode'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;fire-drill;help'= {
            cand metadata-rotation 'metadata-rotation'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;fire-drill;help;metadata-rotation'= {
        }
        &'sui;fire-drill;help;help'= {
        }
        &'sui;analyzer'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;analyze-trace'= {
            cand -p 'The path to the trace file to analyze'
            cand --path 'The path to the trace file to analyze'
            cand -o 'The output directory for any generated artifacts. Defaults `<cur_dir>`'
            cand --output-dir 'The output directory for any generated artifacts. Defaults `<cur_dir>`'
            cand -h 'Print help'
            cand --help 'Print help'
            cand gas-profile 'Generate a gas profile for the trace file compatible with the `speedscope.app` profiler'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;analyze-trace;gas-profile'= {
            cand -u 'Whether function names should be fully qualified with their module and package addresses or if only the function name should be used'
            cand --use-long-function-name 'Whether function names should be fully qualified with their module and package addresses or if only the function name should be used'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;analyze-trace;help'= {
            cand gas-profile 'Generate a gas profile for the trace file compatible with the `speedscope.app` profiler'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;analyze-trace;help;gas-profile'= {
        }
        &'sui;analyze-trace;help;help'= {
        }
        &'sui;replay'= {
            cand --client.config 'Sets the file storing the state of our user accounts (an empty one will be created if missing)'
            cand --client.env 'The Sui environment to use. This must be present in the current config file'
            cand -d 'Transaction digest to replay'
            cand --digest 'Transaction digest to replay'
            cand --digests-path 'File containing a list of digests, one per line'
            cand -o 'The output directory for the replay artifacts. Defaults `<cur_dir>/.replay/<digest>`'
            cand --output-dir 'The output directory for the replay artifacts. Defaults `<cur_dir>/.replay/<digest>`'
            cand -e 'Show transaction effects'
            cand --show-effects 'Show transaction effects'
            cand --terminate-early 'Terminate a batch replay early if an error occurs when replaying one of the transactions'
            cand --trace 'Whether to trace the transaction execution. Generated traces will be saved in the output directory (or `<cur_dir>/.replay/<digest>` if none provided)'
            cand --overwrite 'Whether existing artifacts that were generated from a previous replay of the transaction should be overwritten or an error raised if they already exist'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;completion'= {
            cand --generate 'If provided, outputs the completion file for given shell'
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'sui;help'= {
            cand start 'Start a local network in two modes: saving state between re-runs and not saving state between re-runs. Please use (--help) to see the full description.'
            cand network 'network'
            cand genesis 'Bootstrap and initialize a new sui network'
            cand genesis-ceremony 'genesis-ceremony'
            cand keytool 'Sui keystore tool'
            cand client 'Client for interacting with the Sui network'
            cand validator 'A tool for validators and validator candidates'
            cand move 'Tool to build and test Move applications'
            cand bridge-committee-init 'Command to initialize the bridge committee, usually used when running local bridge cluster'
            cand fire-drill 'Tool for Fire Drill'
            cand analyzer 'Invoke Sui''s move-analyzer via CLI'
            cand analyze-trace 'Analyze and/or transform a trace file'
            cand replay 'Arguments for replay (used for both CLI and config file)'
            cand completion 'Generate shell completion scripts for CLI'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'sui;help;start'= {
        }
        &'sui;help;network'= {
        }
        &'sui;help;genesis'= {
        }
        &'sui;help;genesis-ceremony'= {
            cand init 'init'
            cand validate-state 'validate-state'
            cand add-validator 'add-validator'
            cand list-validators 'list-validators'
            cand build-unsigned-checkpoint 'build-unsigned-checkpoint'
            cand examine-genesis-checkpoint 'examine-genesis-checkpoint'
            cand verify-and-sign 'verify-and-sign'
            cand finalize 'finalize'
        }
        &'sui;help;genesis-ceremony;init'= {
        }
        &'sui;help;genesis-ceremony;validate-state'= {
        }
        &'sui;help;genesis-ceremony;add-validator'= {
        }
        &'sui;help;genesis-ceremony;list-validators'= {
        }
        &'sui;help;genesis-ceremony;build-unsigned-checkpoint'= {
        }
        &'sui;help;genesis-ceremony;examine-genesis-checkpoint'= {
        }
        &'sui;help;genesis-ceremony;verify-and-sign'= {
        }
        &'sui;help;genesis-ceremony;finalize'= {
        }
        &'sui;help;keytool'= {
            cand update-alias 'Update an old alias to a new one. If a new alias is not provided, a random one will be generated'
            cand convert 'Convert private key in Hex or Base64 to new format (Bech32 encoded 33 byte flag || private key starting with "suiprivkey"). Hex private key format import and export are both deprecated in Sui Wallet and Sui CLI Keystore. Use `sui keytool import` if you wish to import a key to Sui Keystore'
            cand decode-or-verify-tx 'Given a Base64 encoded transaction bytes, decode its components. If a signature is provided, verify the signature against the transaction and output the result'
            cand decode-multi-sig 'Given a Base64 encoded MultiSig signature, decode its components. If tx_bytes is passed in, verify the multisig'
            cand generate 'Generate a new keypair with key scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand import 'Add a new key to Sui CLI Keystore using either the input mnemonic phrase or a Bech32 encoded 33-byte `flag || privkey` starting with "suiprivkey", the key scheme flag {ed25519 | secp256k1 | secp256r1} and an optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Supports mnemonic phrase of word length 12, 15, 18, 21, 24. Set an alias for the key with the --alias flag. If no alias is provided, the tool will automatically generate one'
            cand export 'Output the private key of the given key identity in Sui CLI Keystore as Bech32 encoded string starting with `suiprivkey`'
            cand list 'List all keys by its Sui address, Base64 encoded public key, key scheme name in sui.keystore'
            cand load-keypair 'This reads the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). This prints out the account keypair as Base64 encoded `flag || privkey`, the network keypair, worker keypair, protocol keypair as Base64 encoded `privkey`'
            cand multi-sig-address 'To MultiSig Sui Address. Pass in a list of all public keys `flag || pk` in Base64. See `keytool list` for example public keys'
            cand multi-sig-combine-partial-sig 'Provides a list of participating signatures (`flag || sig || pk` encoded in Base64), threshold, a list of all public keys and a list of their weights that define the MultiSig address. Returns a valid MultiSig signature and its sender address. The result can be used as signature field for `sui client execute-signed-tx`. The sum of weights of all signatures must be >= the threshold'
            cand multi-sig-combine-partial-sig-legacy 'multi-sig-combine-partial-sig-legacy'
            cand show 'Read the content at the provided file path. The accepted format can be [enum SuiKeyPair] (Base64 encoded of 33-byte `flag || privkey`) or `type AuthorityKeyPair` (Base64 encoded `privkey`). It prints its Base64 encoded public key and the key scheme flag'
            cand sign 'Create signature using the private key for the given address (or its alias) in sui keystore. Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand sign-kms 'Creates a signature by leveraging AWS KMS. Pass in a key-id to leverage Amazon KMS to sign a message and the base64 pubkey. Generate PubKey from pem using MystenLabs/base64pemkey Any signature commits to a [struct IntentMessage] consisting of the Base64 encoded of the BCS serialized transaction bytes itself and its intent. If intent is absent, default will be used'
            cand unpack 'This takes [enum SuiKeyPair] of Base64 encoded of 33-byte `flag || privkey`). It outputs the keypair into a file at the current directory where the address is the filename, and prints out its Sui address, Base64 encoded public key, the key scheme, and the key scheme flag'
            cand zk-login-sign-and-execute-tx 'Given the max_epoch, generate an OAuth url, ask user to paste the redirect with id_token, call salt server, then call the prover server, create a test transaction, use the ephemeral key to sign and execute it by assembling to a serialized zkLogin signature'
            cand zk-login-enter-token 'A workaround to the above command because sometimes token pasting does not work (for Facebook). All the inputs required here are printed from the command above'
            cand zk-login-sig-verify 'Given a zkLogin signature, parse it if valid. If `bytes` provided, parse it as either as TransactionData or PersonalMessage based on `intent_scope`. It verifies the zkLogin signature based its latest JWK fetched. Example request: sui keytool zk-login-sig-verify --sig $SERIALIZED_ZKLOGIN_SIG --bytes $BYTES --intent-scope 0 --network devnet --curr-epoch 10'
            cand zk-login-insecure-sign-personal-message 'TESTING ONLY: Generate a fixed ephemeral key and its JWT token with test issuer. Produce a zklogin signature for the given data and max epoch. e.g. sui keytool zk-login-insecure-sign-personal-message --data "hello" --max-epoch 5'
        }
        &'sui;help;keytool;update-alias'= {
        }
        &'sui;help;keytool;convert'= {
        }
        &'sui;help;keytool;decode-or-verify-tx'= {
        }
        &'sui;help;keytool;decode-multi-sig'= {
        }
        &'sui;help;keytool;generate'= {
        }
        &'sui;help;keytool;import'= {
        }
        &'sui;help;keytool;export'= {
        }
        &'sui;help;keytool;list'= {
        }
        &'sui;help;keytool;load-keypair'= {
        }
        &'sui;help;keytool;multi-sig-address'= {
        }
        &'sui;help;keytool;multi-sig-combine-partial-sig'= {
        }
        &'sui;help;keytool;multi-sig-combine-partial-sig-legacy'= {
        }
        &'sui;help;keytool;show'= {
        }
        &'sui;help;keytool;sign'= {
        }
        &'sui;help;keytool;sign-kms'= {
        }
        &'sui;help;keytool;unpack'= {
        }
        &'sui;help;keytool;zk-login-sign-and-execute-tx'= {
        }
        &'sui;help;keytool;zk-login-enter-token'= {
        }
        &'sui;help;keytool;zk-login-sig-verify'= {
        }
        &'sui;help;keytool;zk-login-insecure-sign-personal-message'= {
        }
        &'sui;help;client'= {
            cand active-address 'Default address used for commands when none specified'
            cand active-env 'Default environment used for commands when none specified'
            cand addresses 'Obtain the Addresses managed by the client'
            cand balance 'List the coin balance of an address'
            cand call 'Call Move function'
            cand chain-identifier 'Query the chain identifier from the rpc endpoint'
            cand dynamic-field 'Query a dynamic field by its address'
            cand envs 'List all Sui environments'
            cand execute-signed-tx 'Execute a Signed Transaction. This is useful when the user prefers to sign elsewhere and use this command to execute'
            cand execute-combined-signed-tx 'Execute a combined serialized SenderSignedData string'
            cand faucet 'Request gas coin from faucet. By default, it will use the active address and the active network'
            cand gas 'Obtain all gas objects owned by the address. An address'' alias can be used instead of the address'
            cand merge-coin 'Merge two coin objects into one coin'
            cand new-address 'Generate new address and keypair with keypair scheme flag {ed25519 | secp256k1 | secp256r1} with optional derivation path, default to m/44''/784''/0''/0''/0'' for ed25519 or m/54''/784''/0''/0/0 for secp256k1 or m/74''/784''/0''/0/0 for secp256r1. Word length can be { word12 | word15 | word18 | word21 | word24} default to word12 if not specified'
            cand new-env 'Add new Sui environment'
            cand object 'Get object info'
            cand objects 'Obtain all objects owned by the address. It also accepts an address by its alias'
            cand party-transfer 'Transfer object to party ownership'
            cand pay 'Pay coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts'
            cand pay-all-sui 'Pay all residual SUI coins to the recipient with input coins, after deducting the gas cost. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand pay-sui 'Pay SUI coins to recipients following specified amounts, with input coins. Length of recipients must be the same as that of amounts. The input coins also include the coin for gas payment, so no extra gas coin is required'
            cand ptb 'Run a PTB from the provided args'
            cand publish 'Publish Move modules'
            cand serialized-tx 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction'
            cand serialized-tx-kind 'Execute, dry-run, dev-inspect or otherwise inspect an already serialized transaction kind'
            cand split-coin 'Split a coin object into multiple coins'
            cand switch 'Switch active address and network(e.g., devnet, local rpc server)'
            cand tx-block 'Get the effects of executing the given transaction block'
            cand transfer 'Transfer object'
            cand transfer-sui 'Transfer SUI, and pay gas with the same SUI coin object. If amount is specified, only the amount is transferred; otherwise the entire object is transferred'
            cand upgrade 'Upgrade Move modules'
            cand verify-bytecode-meter 'Run the bytecode verifier on the package'
            cand verify-source 'Verify local Move packages against on-chain packages, and optionally their dependencies'
            cand remove-address 'Remove an existing address by its alias or hexadecimal string'
            cand replay-transaction 'Replay a given transaction to view transaction effects (deprecated; use `sui replay` instead)'
            cand replay-batch 'Replay transactions listed in a file (deprecated; use `sui replay` instead)'
            cand replay-checkpoint 'Replay all transactions in a range of checkpoints (deprecated; use `sui replay` instead)'
        }
        &'sui;help;client;active-address'= {
        }
        &'sui;help;client;active-env'= {
        }
        &'sui;help;client;addresses'= {
        }
        &'sui;help;client;balance'= {
        }
        &'sui;help;client;call'= {
        }
        &'sui;help;client;chain-identifier'= {
        }
        &'sui;help;client;dynamic-field'= {
        }
        &'sui;help;client;envs'= {
        }
        &'sui;help;client;execute-signed-tx'= {
        }
        &'sui;help;client;execute-combined-signed-tx'= {
        }
        &'sui;help;client;faucet'= {
        }
        &'sui;help;client;gas'= {
        }
        &'sui;help;client;merge-coin'= {
        }
        &'sui;help;client;new-address'= {
        }
        &'sui;help;client;new-env'= {
        }
        &'sui;help;client;object'= {
        }
        &'sui;help;client;objects'= {
        }
        &'sui;help;client;party-transfer'= {
        }
        &'sui;help;client;pay'= {
        }
        &'sui;help;client;pay-all-sui'= {
        }
        &'sui;help;client;pay-sui'= {
        }
        &'sui;help;client;ptb'= {
        }
        &'sui;help;client;publish'= {
        }
        &'sui;help;client;serialized-tx'= {
        }
        &'sui;help;client;serialized-tx-kind'= {
        }
        &'sui;help;client;split-coin'= {
        }
        &'sui;help;client;switch'= {
        }
        &'sui;help;client;tx-block'= {
        }
        &'sui;help;client;transfer'= {
        }
        &'sui;help;client;transfer-sui'= {
        }
        &'sui;help;client;upgrade'= {
        }
        &'sui;help;client;verify-bytecode-meter'= {
        }
        &'sui;help;client;verify-source'= {
        }
        &'sui;help;client;remove-address'= {
        }
        &'sui;help;client;replay-transaction'= {
        }
        &'sui;help;client;replay-batch'= {
        }
        &'sui;help;client;replay-checkpoint'= {
        }
        &'sui;help;validator'= {
            cand make-validator-info 'make-validator-info'
            cand become-candidate 'Arguments related to transaction processing'
            cand join-committee 'Arguments related to transaction processing'
            cand leave-committee 'Arguments related to transaction processing'
            cand display-metadata 'display-metadata'
            cand update-metadata 'Arguments related to transaction processing'
            cand update-gas-price 'Update gas price that is used to calculate Reference Gas Price'
            cand report-validator 'Report or un-report a validator. Report or un-report a validator'
            cand serialize-payload-pop 'Serialize the payload that is used to generate Proof of Possession. This is useful to take the payload offline for an Authority protocol keypair to sign'
            cand display-gas-price-update-raw-txn 'Print out the serialized data of a transaction that sets the gas price quote for a validator'
            cand register-bridge-committee 'Sui native bridge committee member registration'
            cand update-bridge-committee-node-url 'Update sui native bridge committee node url'
        }
        &'sui;help;validator;make-validator-info'= {
        }
        &'sui;help;validator;become-candidate'= {
        }
        &'sui;help;validator;join-committee'= {
        }
        &'sui;help;validator;leave-committee'= {
        }
        &'sui;help;validator;display-metadata'= {
        }
        &'sui;help;validator;update-metadata'= {
            cand name 'Update name. Effectuate immediately'
            cand description 'Update description. Effectuate immediately'
            cand image-url 'Update Image URL. Effectuate immediately'
            cand project-url 'Update Project URL. Effectuate immediately'
            cand network-address 'Update Network Address. Effectuate from next epoch'
            cand primary-address 'Update Primary Address. Effectuate from next epoch'
            cand worker-address 'Update Worker Address. Effectuate from next epoch'
            cand p2p-address 'Update P2P Address. Effectuate from next epoch'
            cand network-pub-key 'Update Network Public Key. Effectuate from next epoch'
            cand worker-pub-key 'Update Worker Public Key. Effectuate from next epoch'
            cand protocol-pub-key 'Update Protocol Public Key and Proof and Possession. Effectuate from next epoch'
        }
        &'sui;help;validator;update-metadata;name'= {
        }
        &'sui;help;validator;update-metadata;description'= {
        }
        &'sui;help;validator;update-metadata;image-url'= {
        }
        &'sui;help;validator;update-metadata;project-url'= {
        }
        &'sui;help;validator;update-metadata;network-address'= {
        }
        &'sui;help;validator;update-metadata;primary-address'= {
        }
        &'sui;help;validator;update-metadata;worker-address'= {
        }
        &'sui;help;validator;update-metadata;p2p-address'= {
        }
        &'sui;help;validator;update-metadata;network-pub-key'= {
        }
        &'sui;help;validator;update-metadata;worker-pub-key'= {
        }
        &'sui;help;validator;update-metadata;protocol-pub-key'= {
        }
        &'sui;help;validator;update-gas-price'= {
        }
        &'sui;help;validator;report-validator'= {
        }
        &'sui;help;validator;serialize-payload-pop'= {
        }
        &'sui;help;validator;display-gas-price-update-raw-txn'= {
        }
        &'sui;help;validator;register-bridge-committee'= {
        }
        &'sui;help;validator;update-bridge-committee-node-url'= {
        }
        &'sui;help;move'= {
            cand build 'build'
            cand coverage 'Inspect test coverage for this package. A previous test run with the `--coverage` flag must have previously been run'
            cand disassemble 'disassemble'
            cand manage-package 'Record addresses (Object IDs) for where this package is published on chain (this command sets variables in Move.lock)'
            cand migrate 'Migrate to Move 2024 for the package at `path`. If no path is provided defaults to current directory'
            cand new 'Create a new Move package with name `name` at `path`. If `path` is not provided the package will be created in the directory `name`'
            cand test 'Run Move unit tests in this package'
            cand summary 'Generate a serialized summary of a Move package (e.g., functions, structs, annotations, etc.)'
        }
        &'sui;help;move;build'= {
        }
        &'sui;help;move;coverage'= {
            cand summary 'Display a coverage summary for all modules in this package'
            cand source 'Display coverage information about the module against source code'
            cand bytecode 'Display coverage information about the module against disassembled bytecode'
            cand lcov 'Generate LCOV coverage information for the package. Requires traces to be present. Run tests with `--trace` to generate traces'
        }
        &'sui;help;move;coverage;summary'= {
        }
        &'sui;help;move;coverage;source'= {
        }
        &'sui;help;move;coverage;bytecode'= {
        }
        &'sui;help;move;coverage;lcov'= {
        }
        &'sui;help;move;disassemble'= {
        }
        &'sui;help;move;manage-package'= {
        }
        &'sui;help;move;migrate'= {
        }
        &'sui;help;move;new'= {
        }
        &'sui;help;move;test'= {
        }
        &'sui;help;move;summary'= {
        }
        &'sui;help;bridge-committee-init'= {
        }
        &'sui;help;fire-drill'= {
            cand metadata-rotation 'metadata-rotation'
        }
        &'sui;help;fire-drill;metadata-rotation'= {
        }
        &'sui;help;analyzer'= {
        }
        &'sui;help;analyze-trace'= {
            cand gas-profile 'Generate a gas profile for the trace file compatible with the `speedscope.app` profiler'
        }
        &'sui;help;analyze-trace;gas-profile'= {
        }
        &'sui;help;replay'= {
        }
        &'sui;help;completion'= {
        }
        &'sui;help;help'= {
        }
    ]
    $completions[$command]
}
